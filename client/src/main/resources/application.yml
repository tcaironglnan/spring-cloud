spring:
  application:
    name: service-hello

server:
  port: 8762

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka/


logging:
  level:
    root: INFO
    org:
      springframework:
        web: ERROR
      hibernate: ERROR
      mybatis: ERROR
  path: D:/logs/
  file: ./logs/client.log